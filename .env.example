# ============================================================================
# SerpApi Diagnostic Workbench - Environment Configuration
# ============================================================================
# Copy this file to .env and fill in your values
# cp .env.example .env
# ============================================================================

# ----------------------------------------------------------------------------
# SERVER CONFIGURATION
# ----------------------------------------------------------------------------

# Environment mode: development | production | test
NODE_ENV=development

# Backend server port
PORT=3001

# Application URL (required for production - used for CORS and trusted origins)
# Development: leave empty (uses localhost defaults)
# Production:  https://your-domain.com
# APP_URL=https://your-domain.com

# ----------------------------------------------------------------------------
# DATABASE - TURSO (Required)
# ----------------------------------------------------------------------------
# Turso is a SQLite-compatible database for the edge
# 
# Setup:
#   1. Install CLI:    curl -sSfL https://get.tur.so/install.sh | bash
#   2. Login:          turso auth login
#   3. Create DB:      turso db create your-app-name
#   4. Get URL:        turso db show your-app-name --url
#   5. Get Token:      turso db tokens create your-app-name
#   6. Push Schema:    bun run db:push
#
# Dashboard: https://turso.tech/app
# Docs:      https://docs.turso.tech
# ----------------------------------------------------------------------------

# Database URL (format: libsql://[database-name]-[username].turso.io)
TURSO_DATABASE_URL=libsql://your-database.turso.io

# Database auth token (starts with "eyJ...")
TURSO_AUTH_TOKEN=your-turso-auth-token

# ----------------------------------------------------------------------------
# EMAIL - RESEND
# ----------------------------------------------------------------------------
# Setup: https://resend.com
#   1. Create account
#   2. Get API key from dashboard
#   3. Verify your domain for production
# ----------------------------------------------------------------------------

# Resend API key (starts with "re_...")
# RESEND_API_KEY=re_xxxxxxxxxx

# Email sender address (must be verified domain in production)
#EMAIL_FROM=noreply@serpapi-diagnostic-workbench.rokimiftah.id

# ----------------------------------------------------------------------------
# SERPAPI CONFIGURATION (Required for Diagnostics)
# ----------------------------------------------------------------------------
# SerpApi is used for search engine result scraping
#
# Setup: https://serpapi.com/manage-api-key
# ----------------------------------------------------------------------------

# SerpApi API Key
SERPAPI_API_KEY=your-serpapi-api-key

# ----------------------------------------------------------------------------
# MONITORING CONFIGURATION (Optional - for Phase 2)
# ----------------------------------------------------------------------------
# Proactive monitoring settings for automated diagnostics
# ----------------------------------------------------------------------------

# Enable/disable scheduled monitoring (default: false)
MONITORING_ENABLED=false

# Interval between monitoring runs in hours (default: 1)
MONITORING_INTERVAL_HOURS=1

# Success rate threshold - alert if below this percentage (default: 80)
SUCCESS_RATE_THRESHOLD=80

# Error rate threshold - alert if above this percentage (default: 30)
ERROR_RATE_THRESHOLD=30

# Latency threshold in milliseconds - alert if above this (default: 10000)
LATENCY_THRESHOLD_MS=10000
